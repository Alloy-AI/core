{
    "name": "alloy",
    "module": "index.ts",
    "type": "module",
    "private": true,
    "workspaces": [
        "packages/contracts",
        "packages/server",
        "packages/service"
    ],
    "scripts": {
        "client:dev": "bun run --cwd packages/server dev",
        "client:build": "bun run --cwd packages/server build",
        "client:start": "bun run --cwd packages/server start"
    },
    "devDependencies": {
        "@types/bun": "latest"
    },
    "peerDependencies": {
        "typescript": "^5"
    }
}
